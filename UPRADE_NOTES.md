Upgrade visibility of Time of Test: Make a timeline of tests and their dates more prominent on the test models page so people can see when the test was run.

Bulk Tester: Make an upgrade to the runner or a variant on the runner that allows for the retesting of all the models in the benchmark system, and then an automatic resubmission of those models for a second test run. This bulk tester should be able to pull the full list of available models, but also be able to deselect ones you don't want to run again. 

DONE - Change the test score to be the most recent test. This is a change from scoring across the average, but the purpose is that the models themselves change. Therefore, the most recent test could show that a model has become less compliant. This change in scoring logic will just depend on the most recent test of that model, and this will make the timeline and the bulk teer more relevant. We might even bulk test monthly. 

Add a sponsors tab in the about section and add Gospel Ambition, Visual Story Network, Digital Disciple Making Network, and Prediction Guard.