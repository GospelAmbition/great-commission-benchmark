# Specify Node.js 20 for Next.js 16 compatibility
[variables]
NODE_VERSION = "20"
NIXPACKS_NODE_VERSION = "20"

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
