"""Payments API endpoints"""
from fastapi import APIRouter

# Payments endpoint is now empty - all test-related payment endpoints have been removed
# Payment processing for CLI submissions and sponsorships is handled directly in their respective endpoints
router = APIRouter()
